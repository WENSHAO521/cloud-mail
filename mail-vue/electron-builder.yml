appId: com.psg.mail
productName: PSG Mail
copyright: Copyright © 2025 Panorama Scholarly Group

directories:
  output: dist-electron
  buildResources: build

# What to bundle
files:
  - dist/**
  - electron/**
  - package.json

# Icons outside asar so Windows/Linux system APIs can access them as real files
extraResources:
  - from: build/icon.ico
    to: build/icon.ico
  - from: build/icon.icns
    to: build/icon.icns
  - from: build/icon.png
    to: build/icon.png

# ── Windows ──────────────────────────────────────────────────
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.ico

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: PSG Mail
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  include: build/installer.nsh

# ── macOS ─────────────────────────────────────────────────────
mac:
  target:
    - target: dmg
      arch: [universal]
    - target: zip
      arch: [universal]
  icon: build/icon.icns
  category: public.app-category.productivity
  # For unsigned local builds — remove these for App Store / notarized distribution
  identity: null
  hardenedRuntime: false
  gatekeeperAssess: false

dmg:
  title: PSG Mail ${version}
  icon: build/icon.icns
  iconSize: 80
  contents:
    - x: 180
      y: 170
      type: file
    - x: 480
      y: 170
      type: link
      path: /Applications
  window:
    width: 660
    height: 400

# ── Linux ─────────────────────────────────────────────────────
linux:
  target:
    - target: deb
      arch: [x64]
  icon: build/icon.png
  category: Network
  maintainer: Panorama Scholarly Group <admin@panorama-sg.com>

# ── Auto-update publish target ─────────────────────────────────
publish:
  provider: github
  owner: WENSHAO521
  repo: cloud-mail
  releaseType: draft
